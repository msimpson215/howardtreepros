# Render Blueprint — sync with existing service name in dashboard if different
services:
  - type: web
    name: howard-tree-pros
    runtime: docker
    plan: free
    branch: main
    autoDeployTrigger: commit
    healthCheckPath: /
